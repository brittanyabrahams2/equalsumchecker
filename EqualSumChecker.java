public class EqualSumChecker {

    public static boolean hasEqualSum (int num1, int num2, int sum) {
      //  boolean check = true;
       // sum = num1 + num2;
        if (sum == num1 + num2) {
            return true;
        } else {
            return false;
        }

    }
}
